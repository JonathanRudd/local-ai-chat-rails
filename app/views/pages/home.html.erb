
 <div class="wrapper">
    <h2>GREETINGS</h2>
    <p></p>
    <ul class="messages">
      <li>Thinking...</li>
    </ul>
    <form class="add-items">
      <input type="text" name="item" placeholder="Comment here" required>
      <input type="submit" value="Ask">
    </form>
  </div>

<script>
  const addItems = document.querySelector('.add-items');
  const itemsList = document.querySelector('.messages');
  const items = JSON.parse(localStorage.getItem('items')) || [];

  // const for gpt api: user messages will be added to this array, and used to call gpt api.
  // Gpt responses will also be added to this array, and both used to populate the ul list

  const messageArray = [];


  function addItem(e) {
    e.preventDefault();
    const text = (this.querySelector('[name=item]')).value;
    const item = {text};

    items.push(item);
    populateList(items, itemsList);
    localStorage.setItem('items', JSON.stringify(items));
    this.reset();

    // also save text to new array which then calls gpt api
    // has to be saved as a hash key value pair, so that the gpt api can be called with the text as an argument

    messageArray.push(text);
    console.log(messageArray);

    reply = fetch_gpt(messageArray);
    console.log(reply);
  }

  function populateList(messages = [], messagesList) {
    messagesList.innerHTML = messages.map((message, i) => {
      return `
        <li>
          <label for="item${i}">${message.text}</label>
        </li>
      `;
    }).join('');
  }

  addItems.addEventListener('submit', addItem);

  populateList(items, itemsList);

</script>

fetch_gpt(messageArray);
